package lv.acodemy;

public class myPractice2 {
    public static void main(String[] args) {
        //Ask the user to enter a password and then use if
        //statements to determine its strength
        //(e.g., weak, medium, strong) based on criteria such as length, special characters, and uppercase letters.
        //If the password is less than 8 characters, lacks special characters, or lacks uppercase letters, it's considered "weak."
        //If the password is between 8 and 11 characters, it's considered "of medium strength."
        //If the password is 12 or more characters, it's considered "strong."

        String password = "Please Enter Password!";
        System.out.println(password);

    }
}
